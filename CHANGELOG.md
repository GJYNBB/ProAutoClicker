# 更新日志

这个文件记录项目的主要公开变更。

格式参考 Keep a Changelog，并遵循语义化版本号。

## [1.2.0] - 2026-04-12

### 新增

- 动作序列列表支持拖拽排序、批量复制、批量删除和更丰富的两行摘要展示。
- 新增 `InputBackend` 抽象层，将控制器与 Win32 输入实现解耦，并为未来 macOS/Linux 后端预留扩展点。
- 新增硬编码紧急停止热键 `Ctrl+Alt+End`，可在全局热键和应用内热键模式下尽量保持监听。
- 新增启动前确认、执行前安全倒计时和 HUD 最近动作显示。
- 新增帮助菜单中的检查更新与提交反馈入口，并支持可选的启动时自动检查更新。

### 变更

- 控制器改为通过依赖注入接收输入后端，测试中可使用假后端避免真实系统输入。
- 配置 schema 升级到 6，保存自动更新检查开关、安全倒计时和启动确认选项。

### 修复

- 修复旧配置缺少热键字段时会被解析为空热键的问题。

## [1.1.0] - 2026-04-12

### 新增

- 引入统一的“动作单元列表”模型，支持高级动作序列配置与逐步执行。
- 鼠标动作新增单击、双击、三连击、长按，键盘动作支持单键与组合键自动触发。
- 每个动作单元都支持随机间隔、随机按住时长、固定坐标抖动、执行次数或运行时长限制。
- 新增动作序列编辑器、动作复制与上下移动，以及独立的动作编辑弹窗。
- 增加浅色 / 深色主题切换、可拖拽 HUD、自定义 HUD 内容顺序与透明度设置。

### 变更

- 主界面重构为“快速设置 / 高级动作序列 / 运行与帮助”三页结构。
- HUD 配置改为可排序列表，并将位置与透明度一起纳入本地持久化配置。
- 发布工作流继续沿用标签触发，并通过 `CHANGELOG.md` 自动生成 release 说明。

### 修复

- 修复暂停后恢复时未重新读取鼠标目标位置的问题。
- 修复窗口字体显示异常、窗口无法竖向拉伸、托盘退出无响应的问题。
- 修复深色主题下部分页面背景未正确套用样式的问题，并将深色配色调整为更柔和的灰色层次。

## [1.0.1] - 2026-04-08

### 新增

- 完成基于 `SendInput` 的鼠标与键盘触发路径迁移。
- 增加可配置的半透明悬浮窗 HUD，支持从运行状态、实际触发频率、累计点击/按键数中选择 1 到 3 项显示。

### 变更

- 移除尚未交付完成的 Windows 任务栏状态展示代码，保留主界面状态面板与 HUD 作为当前可视化入口。
- 将应用版本号提升到 `1.0.1`。

### 修复

- 调整运行态可观察性刷新逻辑，保持 HUD 与主界面状态信息同步。

## [1.0.0] - 2026-04-06

### 新增

- 发布首个面向 Windows 的公开版本。
- 支持鼠标左键、右键、中键自动连点。
- 支持键盘单键与组合键连发。
- 提供两种鼠标目标模式：启动后延时记录当前位置、固定坐标。
- 支持本地预设的保存、加载、导入与导出，格式为 JSON。
- 支持全局热键、仅应用内热键、系统托盘和 PyInstaller 打包。

### 变更

- 将动作模型简化为鼠标模式与键盘模式。
- 根据当前模式自动隐藏无关设置，界面更简洁。
- 将语言切换入口移到顶部菜单栏，与主菜单并列展示。
- 为应用界面和 README 补全简体中文、繁體中文、English 三语支持。
- 启动时恢复上次实际使用的工作配置与界面语言。

### 修复

- 加强损坏配置和异常预设数据的解析容错。
- 修复因窗口初始化未完成而导致的启动与运行时界面问题。
- 增加状态恢复、模式切换、目标参数显隐和多语言行为的回归测试覆盖。
